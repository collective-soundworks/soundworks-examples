{
  "name": "plugin platform w/ devicemotion example",
  "author": "(this example require valid https certificates to work on iOS >= 13)",

  "clients": {
    "player": {
      "target": "browser",
      "default": true
    },
  },

  "htmlTemplateDirectory": ".build/server/tmpl",
}
